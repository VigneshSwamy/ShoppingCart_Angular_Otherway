import { AcceptedReviewPipe } from './accepted-review.pipe';

describe('AcceptedReviewPipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptedReviewPipe();
    expect(pipe).toBeTruthy();
  });
});
